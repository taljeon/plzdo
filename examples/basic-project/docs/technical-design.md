# Technical Design

The example contains only documentation and a local verification script.

## Verification

`scripts/verify` checks that required files exist. The root release gate performs leak scanning before publishing this example.
