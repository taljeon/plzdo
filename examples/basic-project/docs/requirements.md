# Requirements

Build a tiny local utility example that can be verified without network access, credentials, or live services.

## Requirements

- The example must be understandable in under five minutes.
- The verification script must be deterministic.
- The project must not require secrets or external APIs.
