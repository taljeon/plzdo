# Basic Project Example

This directory is the byte-level result of rendering the public project frame for a synthetic project.

```bash
./scripts/verify
```

The example contains no runtime state. A real project keeps `.plzdo/` local and ignored unless selected contracts are intentionally tracked.
